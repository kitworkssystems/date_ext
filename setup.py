#!/usr/bin/env python
from setuptools import setup, find_packages

setup(
    name='kws_date_ext',
    version='1.0.1',
    description='Date and Datetime extended tools',
    packages=find_packages(),
    include_package_data=True,
    install_requires=[],
)
